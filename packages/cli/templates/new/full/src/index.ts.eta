#!/usr/bin/env bun

import { build } from "@seedcli/core";

const cli = build("<%= it.name %>")
	.src(import.meta.dir) // Auto-discovers commands/ and extensions/
	.help()
	.version("<%= it.version %>")
	.create();

await cli.run();
