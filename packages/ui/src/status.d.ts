import type { StatusState } from "./types.js";
/**
 * Render a status line with an icon and color based on state.
 */
export declare function status(label: string, state: StatusState): string;
//# sourceMappingURL=status.d.ts.map